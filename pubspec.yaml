name: pilzbuddy
description: "PilzBuddy — Pilz-Spots auf der Karte festhalten und mit Freunden teilen."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.199.0+288

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Karte (OpenStreetMap, kein API-Key)
  flutter_map: ^8.3.2
  latlong2: ^0.9.1

  # Offline-Karten (Android): Vector-Tiles aus lokalen PMTiles-Dateien.
  # Nur Beta-Versionen von vector_map_tiles unterstützen flutter_map 8 —
  # deshalb exakt gepinnt. 9.0.0-beta.9 nutzt bewusst noch den stabilen
  # Canvas-Renderer (die 10er-Linie zieht den experimentellen GPU-Stack
  # samt Native-Builds nach sich); Online-OSM bleibt immer Fallback.
  vector_map_tiles: 9.0.0-beta.9
  # Direkt (statt nur transitiv über vector_map_tiles), damit
  # `worthReporting` in lib/core/errors.dart typisiert auf
  # CancellationException prüfen kann statt auf den Klassennamen als
  # Zeichenkette. Dieselbe Schranke wie dort, damit die Auflösung nicht
  # auseinanderläuft.
  executor_lib: ^1.1.1
  vector_tile_renderer: ^6.1.0
  pmtiles: ^1.3.0
  # Zweite Karten-Engine (nativer GPU-Renderer maplibre-native, Android),
  # hinter der MapView-Fassade und einem Opt-in-Schalter — Migrationsplan
  # „Lupo → Porsche". 0.x und jung, deshalb exakt gepinnt; Upgrades nur
  # bewusst. Web sieht das Paket nie (bedingter Import in
  # lib/features/map/map_view/map_view.dart).
  maplibre: 0.3.5
  path_provider: ^2.1.5
  # Zwischenspeicher im Browser (#385): IndexedDB hinter einer
  # plattformneutralen Schnittstelle. Direkt statt nur transitiv über
  # vector_map_tiles — dieselbe Begründung wie bei executor_lib: Der Code
  # baut darauf auf, und die Auflösung soll nicht an einer exakt
  # gepinnten Beta hängen. Der Nebengewinn ist die Testbarkeit:
  # `newIdbFactoryMemory()` fährt dieselbe Implementierung auf der
  # Dart-VM, wo es keinen Browser gibt.
  idb_shim: ^2.9.9
  # Die Browser-Schnittstellen (`navigator.storage`) für den Ausgangskorb
  # im Web (#386) — reine Bindungen des Dart-Teams, im Android-Build nie
  # kompiliert (bedingter Import in lib/data/browser_storage.dart).
  # Direkt aus demselben Grund wie idb_shim.
  web: ^1.1.1
  connectivity_plus: ^7.3.1
  crypto: ^3.0.6
  # Hält den Prozess während des Karten-Downloads wach (Foreground-Service,
  # Android). Kein Web-Support — deshalb nur über bedingten Import in
  # lib/features/offline_maps/download_keep_alive.dart eingebunden.
  flutter_foreground_task: ^11.0.3

  # Standort (GPS)
  geolocator: ^14.0.2

  # Backend (Auth + Datenbank)
  supabase_flutter: ^2.17.2

  # State-Management & Routing
  flutter_riverpod: ^3.4.3
  go_router: ^18.0.1

  # Datumsformate & Statistik-Charts
  intl: ^0.20.2
  fl_chart: ^1.1.1
  share_plus: ^13.3.0
  package_info_plus: ^10.2.1
  url_launcher: ^6.3.2
  http: ^1.6.0

  # GPX/KML-Import & GPX-Export der eigenen Spots
  file_selector: ^1.0.3

  # Fundfotos (#532): Aufnahme und Auswahl. Offizielles Flutter-Paket,
  # KEINE neue Android-Berechtigung — Kamera-Intent und Photo Picker
  # kommen ohne aus, und das Plugin-Manifest deklariert keine (geprüft
  # am 2026-09-22). Was es NICHT tut: Metadaten entfernen. Seine
  # Android-Verkleinerung kopiert im Gegenteil alle 30 GPS-Tags ins
  # verkleinerte Bild zurück (`ExifDataCopier.java`). Deshalb kodiert
  # `lib/core/photo_pipeline.dart` selbst neu und prüft das Ergebnis.
  image_picker: ^1.2.0
  # Der Kodierer dafür — reines Dart, läuft im Isolate und auf der
  # Test-VM. Bisher nur transitiv (flutter_launcher_icons); direkt, weil
  # eigener Code darauf baut und die Auflösung nicht an einem
  # Dev-Werkzeug hängen soll.
  image: ^4.3.0
  xml: ^6.5.0
  archive: ^3.4.10

  # Gerätelokale Einstellungen (lib/core/settings.dart). Erste ihrer Art im
  # Projekt: Bis Issue #145 überlebte keine Einstellung den App-Start.
  # Kein neuer Code im Binary — supabase_flutter und flutter_foreground_task
  # ziehen das Paket ohnehin herein; direkt eingetragen, damit die eigene
  # Nutzung eine eigene Schranke hat und nicht an fremden Constraints hängt.
  shared_preferences: ^2.5.5

  # Push-Benachrichtigungen (#277) — die EINZIGE Firebase-Nutzung im
  # Projekt, Android und Web. Ausschließlich Cloud Messaging: kein
  # Analytics, kein Crashlytics, keine Datenbank. Was die App an Fehlern
  # meldet, geht weiter nach `public.error_reports`; ein zweiter
  # Empfänger wäre eine zweite Wahrheit und ein weiterer
  # Auftragsverarbeiter in der Datenschutzerklärung.
  #
  # Push ist ein NEBENFEATURE: Jeder Fehlerpfad in `push_messaging.dart`
  # endet in „kein Token", und ein kaputtes Firebase-Projekt darf die App
  # nicht am Starten hindern — dieselbe Linie wie beim Update-Check.
  firebase_core: ^4.15.0
  firebase_messaging: ^16.7.0

  # Funde an iNaturalist melden (#553) — und über iNaturalist an GBIF.
  # `flutter_web_auth_2` öffnet die Anmeldung in einem Custom Tab und
  # fängt die Rückleitung ab (eigene CallbackActivity im Manifest); ein
  # selbstgebauter Weg über `url_launcher` hieße neuer Kotlin-Code in
  # `MainActivity.kt`, der Datei ohne Test-Netz. Keine neue Berechtigung.
  # `flutter_secure_storage` hält den iNaturalist-Zugang: Er darf im
  # Namen des Nutzers Beobachtungen anlegen und gehört deshalb in den
  # Keystore, nicht in die SharedPreferences der Einstellungen — und nie
  # nach Supabase.
  flutter_web_auth_2: ^5.1.0
  flutter_secure_storage: ^11.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  image_path: "assets/icon/icon.png"
  android: true
  ios: false
  web:
    generate: true
  adaptive_icon_background: "#2E7D32"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    # Änderungsliste für „Was ist neu" im Profil. Mitgeliefert statt aus dem
    # Netz geholt, damit sie ohne Empfang lesbar bleibt — und weil sie die
    # installierte Version beschreibt, nicht die neueste im Netz.
    # ACHTUNG: Damit steckt eine *.md-Datei im Binary. Der Version Guard in
    # ci.yml nimmt Markdown sonst aus und macht für genau diese eine Datei
    # eine Ausnahme.
    - CHANGELOG.md
    # Die Artfotos zu den Verwechslungspaaren (#511). Elf Bilder von
    # Wikimedia Commons, quadratisch auf 700 px, zusammen ~0,85 MB —
    # geholt mit `tool/species_photos.py`. Die Namensnennung steht in
    # `lib/core/species_photos.dart` und wird daraus auf die Lizenzseite
    # gespiegelt; `test/flows/license_flow_test.dart` wacht darüber.
    - assets/species/
    # MapLibre-Style für die Offline-Karte (generiert aus @protomaps/basemaps,
    # Flavor LIGHT, deutsche Labels — siehe lib/features/offline_maps/).
    - assets/map_style/protomaps_light_de.json
    # Eingebaute Übersichts-Basiskarte (DACH, Zoom 0–7, ~9 MB): Fallback im
    # Offline-Modus für Bereiche ohne installierte Regionskarte. Generiert mit
    # `pmtiles extract <Protomaps-Daily-Build> --bbox=5.5,45.5,17.5,55.5 --maxzoom=7`.
    - assets/offline_maps/overview_dach.pmtiles
    # Schriftzeichen (Glyph-PBFs) für die MapLibre-Engine: selbst erzeugte
    # Noto-Sans-Stacks (SIL OFL, siehe assets/map_glyphs/OFL.txt), fünf
    # Unicode-Bereiche — reicht für deutsche Kartenbeschriftung. MapLibre
    # lädt Glyphs nur von Platte/URL, deshalb werden sie beim Start ins
    # App-Verzeichnis materialisiert (maplibre_style_provider.dart).
    - assets/map_glyphs/noto-sans-regular/
    - assets/map_glyphs/noto-sans-medium/
    # Der OFL-Text zu genau diesen Glyphen. Eigene Zeile, weil die beiden
    # Verzeichnis-Einträge darüber ihn NICHT mitnehmen — Flutter erfasst
    # damit nur Dateien direkt im jeweiligen Verzeichnis, und OFL.txt liegt
    # eine Ebene höher. Bis 1.61.1 fehlte die Zeile: Die Schrift wurde
    # ausgeliefert, ihre Lizenz nicht. Die OFL verlangt aber genau das.
    - assets/map_glyphs/OFL.txt
    # Waldtypen-Gitter (#213): Copernicus HRL Dominant Leaf Type 2024,
    # 250-m-Zellen für DACH (~5,8 MB, bewusst so beschlossen — volle
    # Nadelanteil-Prozente statt kleinerer Klassenwerte). Erzeugt vom
    # Workflow forest-data.yml (tool/forest_grid.py); ein Update ist
    # ein neues Artefakt + Versions-Bump, nie ein Handstand hier.
    - assets/forest/forest_grid.bin.gz
    - assets/forest/forest_manifest.json
    # Baumarten-Gitter (#227): DLR „Tree Species Germany" 2022, CC BY 4.0,
    # dasselbe Hex-Raster wie oben, nur Deutschland (~2,1 MB). Erzeugt vom
    # Workflow forest-species.yml (tool/forest_species.py). Zwei Halbbytes
    # je Zelle — führende Laub- und Nadelart; die häufigste Art allein
    # verschluckte in 17,6 % der Waldzellen den Mischpartner.
    - assets/forest/forest_species.bin.gz
    - assets/forest/forest_species_manifest.json

    # Höhengitter (#279-Rest): Copernicus DEM GLO-90, dasselbe Hex-Raster
    # wie oben, DACH (~3,4 MB). Erzeugt vom Workflow elevation-data.yml
    # (tool/elevation_grid.py). Ein Byte je Zelle in 20-m-Stufen — die
    # Spothöhe, auf die Blatt-Ampel und Kartenfläche die
    # Stationstemperatur umrechnen (0,65 K je 100 m).
    - assets/elevation/elevation.bin.gz
    - assets/elevation/elevation_manifest.json
    # Gemeldete Fundorte aus GBIF (#467), gebaut von tool/gbif_finds.py
    # aus dem lokalen Download; Prüfsumme in tool/generated_assets.json.
    - assets/gbif/gbif_finds.bin.gz
    - assets/gbif/gbif_finds_manifest.json

  # Roboto, mitgeliefert statt von Google geholt (#393).
  #
  # Im BROWSER lud Flutter die Standardschrift bei jedem Seitenaufruf von
  # `fonts.gstatic.com` — vor der Anmeldung, vor jeder Zustimmung, bei
  # jedem Besucher. Gemessen beim Bau von #387; abgestellt ist es erst
  # hier. (Mit CanvasKit geht es nicht ohne Schriftdatei: Der Renderer
  # kann keine Systemschriften benutzen, deshalb bündeln statt einen
  # Font-Stack zu setzen.)
  #
  # **Dieser Block IST der Schalter — im Theme steht nichts.** Gemessen,
  # weil der erste Entwurf zusätzlich ein `fontFamily: kIsWeb ? 'Roboto'`
  # gesetzt hatte: Ohne diese Zeile bleibt gstatic trotzdem weg, ohne die
  # Dateien hier kommt es zurück. Flutters Web-Engine nimmt eine
  # mitgelieferte Familie namens „Roboto" von selbst. Ein `fontFamily`
  # daneben hätte behauptet, es zu bewirken.
  #
  # Und weil das Theme unangetastet bleibt, ändert sich auf ANDROID
  # nichts: Dort passiert der Abruf gar nicht, und die Systemschrift —
  # auf Hersteller-Oberflächen oft nicht Roboto — bleibt stehen.
  #
  # **Drei Schnitte, nicht einer.** 400 und 700 nutzt die App selbst,
  # 500 braucht Material 3 für Titel und Knopfbeschriftungen; ohne den
  # Schnitt fiele er auf 400 zurück und die Oberfläche sähe im Browser
  # dünner aus als auf dem Telefon. w600 (dreimal im Code) rundet auf 500
  # oder 700 — dafür einen vierten Schnitt zu laden wäre 168 KB für einen
  # Unterschied, den man nicht sieht.
  #
  # Die Dateien stammen aus dem Flutter-SDK (`material_fonts`), die
  # Lizenz liegt daneben: Apache-2.0, auf der Erlaubt-Liste in
  # `tool/license_config.yaml`.
  #
  # Sie liegen auch im APK, obwohl Android bei seiner Systemschrift
  # bleibt — Flutter kennt keine plattformabhängigen Asset-Listen
  # (dieselbe Kröte wie `assets/map_glyphs/`, das im Web-Build nie
  # gelesen wird). 500 KB auf 123 MB.
  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Regular.ttf
        - asset: assets/fonts/Roboto-Medium.ttf
          weight: 500
        - asset: assets/fonts/Roboto-Bold.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
